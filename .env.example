# Optional: extraction is disabled until both values are configured server-side.
GEMINI_API_KEY=
GEMINI_MODEL=
# Keep PORT aligned with the proxy in vite.config.js.
PORT=5000
# Durable local backend data. Keep this directory outside source control.
SSC_DATA_DIR=.ssc-data
